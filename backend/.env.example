# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Security Secret
JWT_SECRET=nexus_research_super_secret_jwt_key_2026_hackathon

# Google Gemini API Key (Backend Only - NEVER expose to frontend)
GEMINI_API_KEY=your_google_gemini_api_key_here

# Optional: Supabase Database Integration (Fallback to local SQLite if not provided)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
